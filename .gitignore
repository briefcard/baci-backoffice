node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
coverage/
.vite/

# Showcase seed holds real catalog data — keep it out of the repo.
# On Render the live Admin API token syncs the real catalog instead.
server/seed-snapshot.json
